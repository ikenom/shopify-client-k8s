apiVersion: v1
kind: Service
metadata:
  name: {{ include "ikenom-app.fullname" . }}
  labels:
    {{- include "ikenom-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ikenom-app.selectorLabels" . | nindent 4 }}
