apiVersion: skaffold/v2beta10
kind: Config
build:
  artifacts:
  - image: ghcr.io/ikenom/shopify-client
    sync:
        infer:
        - 'app/**/*.rb'

deploy:
  helm:
    releases:
    - name: shopify-client
      namespace: default
      artifactOverrides:
        image: ghcr.io/ikenom/shopify-client
      chartPath: k8s
      skipBuildDependencies: false
      valuesFiles:
        - ./k8s/values.yaml
      imageStrategy:
        helm: {}